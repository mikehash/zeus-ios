<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  GENERATED FILE — DO NOT EDIT.

  xcodegen writes this whole file from the `targets.Zeus.info.properties`
  block in project.yml on every `xcodegen generate`. MEASURED, not assumed:
  a value mutated here is gone after the next generate, and this comment is
  destroyed along with it (the rewrite is whole-file, not key-wise). Edit
  project.yml instead — it is the authoritative source for every key below,
  including the CFBundleURLTypes block that makes the OS route `zeus://`
  to this app.

  THE PREDICTION ABOVE HAS NOW BEEN OBSERVED, not merely reasoned. The
  UILaunchScreen edit at 35eb4ef required a `xcodegen generate`, and that
  run deleted this entire block — the diff was 4 insertions, 12 deletions,
  and the twelve were these lines. It is re-added BY HAND, knowing it dies
  again on the next generate. That is not a defect to fix here; it is the
  property the note exists to state, and a warning whose lifetime ends with
  the event it warns about is worth re-adding precisely because a reader who
  arrives after a regenerate finds a plist that looks hand-authored.
-->
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleDisplayName</key>
	<string>Zeus</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>1.0</string>
	<key>CFBundleURLTypes</key>
	<array>
		<dict>
			<key>CFBundleTypeRole</key>
			<string>Editor</string>
			<key>CFBundleURLName</key>
			<string>com.zeus.deeplink</string>
			<key>CFBundleURLSchemes</key>
			<array>
				<string>zeus</string>
			</array>
		</dict>
	</array>
	<key>CFBundleVersion</key>
	<string>1</string>
	<key>UILaunchScreen</key>
	<dict>
		<key>UIColorName</key>
		<string>LaunchBackground</string>
	</dict>
</dict>
</plist>
