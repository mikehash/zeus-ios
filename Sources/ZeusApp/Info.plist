<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  GENERATED FILE — DO NOT EDIT.

  xcodegen writes this whole file from the `targets.Zeus.info.properties`
  block in project.yml on every `xcodegen generate`. MEASURED, not assumed:
  a value mutated here is gone after the next generate, and this comment is
  destroyed along with it (the rewrite is whole-file, not key-wise). Edit
  project.yml instead — it is the authoritative source for every key below,
  including the CFBundleURLTypes block that makes the OS route `zeus://`
  to this app.

  THE PREDICTION HAS NOW BEEN OBSERVED THREE TIMES, and the observations
  SHARPEN it past what it originally claimed. At 35eb4ef a generate deleted
  this block whole (4 insertions, 12 deletions, the twelve being these
  lines). At cc494a5 two further generates left the file BYTE-IDENTICAL,
  note intact. This commit killed it twice more — once adding the three keys
  below, once restoring project.yml after the mutation arm.

  So it does not die on INVOCATION. It dies on a CONTENT CHANGE of the
  rendered plist: xcodegen rewrites the file only when the output differs,
  and the rewrite is whole-file, so the comment goes with it. Re-added by
  hand; the next death is the next key someone adds.

  That distinction matters to a reader. "Regenerating destroys this" would
  have them infer, from a note-less plist, that someone recently ran
  generate — which is false. A note survives every generate that changes
  nothing, and dies on every one that changes something.
-->
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleDisplayName</key>
	<string>Zeus</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>1.0</string>
	<key>CFBundleURLTypes</key>
	<array>
		<dict>
			<key>CFBundleTypeRole</key>
			<string>Editor</string>
			<key>CFBundleURLName</key>
			<string>com.zeus.deeplink</string>
			<key>CFBundleURLSchemes</key>
			<array>
				<string>zeus</string>
			</array>
		</dict>
	</array>
	<key>CFBundleVersion</key>
	<string>1</string>
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>
	<key>UILaunchScreen</key>
	<dict>
		<key>UIColorName</key>
		<string>LaunchBackground</string>
	</dict>
	<key>UIRequiredDeviceCapabilities</key>
	<array>
		<string>arm64</string>
	</array>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
	</array>
</dict>
</plist>
